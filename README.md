# orgs

#### Установка зависимостей:

`npm install`

#### Сборка проекта в dev режиме:

`npm run dev`

🚀 Запуск проекта

Открой файл index.html с помощью Live Server

❗ Не открывать напрямую через file:///,
браузер заблокирует модули из-за CORS-политики.
